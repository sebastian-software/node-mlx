{
  "name": "@node-mlx/benchmarks",
  "version": "0.1.0",
  "private": true,
  "description": "Benchmarks for node-mlx vs node-llama-cpp",
  "type": "module",
  "scripts": {
    "benchmark": "tsx src/benchmark.ts",
    "compare": "tsx src/compare.ts",
    "phi4": "tsx src/phi4-compare.ts",
    "gemma3n": "tsx src/gemma3n-compare.ts",
    "mlx-models": "tsx src/mlx-models.ts",
    "build": "echo 'No build step for benchmarks'",
    "clean": "echo 'No clean step for benchmarks'",
    "test": "echo 'Benchmarks have no tests'"
  },
  "dependencies": {
    "node-mlx": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
