{
  "name": "@node-mlx/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for node-mlx - LLM inference powered by Apple MLX",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter node-mlx dev",
    "build": "turbo run build",
    "build:swift": "cd packages/swift && xcodebuild build -scheme NodeMLX -configuration Release -destination 'platform=macOS' && ./copy-artifacts.sh",
    "build:native": "turbo run build:native --filter=node-mlx",
    "clean": "turbo run clean && rm -rf packages/swift/.build coverage .turbo",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:coverage": "turbo run test:coverage",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint -- --fix",
    "hf2swift": "node packages/hf2swift/dist/cli.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "benchmark": "turbo run benchmark --filter=@node-mlx/benchmarks"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@commitlint/types": "^20.0.0",
    "@eslint/js": "^9.0.0",
    "@stylistic/eslint-plugin": "^5.0.0",
    "eslint": "^9.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0",
    "node-gyp": "^11.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
